@model GameStoreData.Models.Game

@{
    // TODO: Add Genre to Edit
    ViewData["Title"] = "Edit";
}

<h1>Edit Game</h1>

<hr />  
<div class="row">
    <div class="col-md-4">
        <form asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Price" class="control-label"></label>
                <input asp-for="Price" class="form-control" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Image" class="control-label"></label>
                <input asp-for="Image" class="form-control" />
                <span asp-validation-for="Image" class="text-danger"></span>
            </div>

     @*       <div class="form-group">
                @Html.LabelFor(model => model.Genres)
                <div class="col-sm-6">
                    @Html.ListBoxFor(model => model.Genres, new MultiSelectList(ViewBag.Genres, "Id", "Name"),
                    new { @class = "form-control", multiple = "multiple", id = "genres_dropdown"})
                </div>
            </div>
*@
            <div class="form-group">
                @Html.LabelFor(model => model.Genres)
                <div class="col-sm-6">
                    @*@Html.ListBoxFor(model => model.Genres, (List<SelectListItem>)ViewBag.Genres)*@
                    @Html.ListBoxFor(model => model.Genres, new MultiSelectList(ViewBag.Genres, "Id", "Name"))
                </div>
            </div>

            <div class="form-group">
                <input type="submit" value="Save" class="newgame" />
                <a asp-action="Index" class="newgame">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
  @*  <script>
        $(document).ready(function () {
            $('#genres_dropdown').multiselect();
        });
    </script>*@
}
